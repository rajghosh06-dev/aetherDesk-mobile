<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Expose the app's cache directory where we save temporary images/PDFs before sharing -->
    <cache-path name="shared_docs" path="." />
    <!-- Expose external downloads directory if necessary -->
    <external-path name="external_files" path="." />
</paths>
